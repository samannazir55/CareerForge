/**
 * interview module — implementation lives in interview.routes.ts, mounted
 * at /api/interview in app.ts. This file is kept as the module's barrel
 * entry point in case shared helpers need a home later.
 */
export {};
