/**
 * linkedin module — implementation lives in linkedin.routes.ts, mounted
 * at /api/linkedin in app.ts. This file is kept as the module's barrel
 * entry point in case shared helpers need a home later.
 */
export {};
